/// The default interval at which the TimerCubit fetches data
const Duration kTimerCubitRefreshInterval = Duration(minutes: 1);
/// The interval at which we check the node sync status
const Duration kNodeSyncStatusRefreshInterval = Duration(seconds: 10);
