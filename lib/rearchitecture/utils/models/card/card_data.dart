/// A class that holds data needed in a card, like the [description] and the
/// [title]
class CardData {
  /// Creates a [CardData] instance.
  CardData({required this.description, required this.title});
  /// The description of a card.
  final String description;
  /// The title of a card.
  final String title;
}
