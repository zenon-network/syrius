/// A class that helps distinguish between the card widgets
enum CardType {

  /// A type for a card displaying the latest transactions for an address
  latestTransactions,

  /// A variant of the [latestTransactions] card
  latestTransactionsDashboard;
}
