import 'package:flutter/material.dart';

class BalanceDashboardEmpty extends StatelessWidget {
  const BalanceDashboardEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('empty');
  }
}