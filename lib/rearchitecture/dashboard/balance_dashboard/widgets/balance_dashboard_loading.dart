import 'package:flutter/material.dart';

class BalanceDashboardLoading extends StatelessWidget {
  const BalanceDashboardLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('loading');
  }
}
